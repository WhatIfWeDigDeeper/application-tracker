See `../angular-ui/CLAUDE.md` for Angular patterns.

See `../spring-api/CLAUDE.md` for Java/Spring patterns. Note: `angular-spring-ui` calls `/api/applications/:id/interview-n` (not `/interview-stages`) — the interview-stage feature is broken by design; do not route-match or rename `/interview-stages` to `/interview-n`.
