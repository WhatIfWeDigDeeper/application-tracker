// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               unknown
// source: history/v1/history.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { handleUnaryCall, UntypedServiceImplementation } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";

export const protobufPackage = "history.v1";

export interface RecordHistoryRequest {
  /** UUID of the application this history entry belongs to. */
  applicationId: string;
  /** Human-readable description of the change (e.g. "Updated Status, Notes"). */
  description: string;
  /** JSON-encoded snapshot of the application state at the time of the change. */
  snapshot: Uint8Array;
}

export interface RecordHistoryResponse {
  /** The sequence number assigned to this history entry. */
  sequence: number;
}

export interface ListHistoryRequest {
  applicationId: string;
  /** 1-based page number. Defaults to 1 if omitted. */
  page: number;
  /** Number of entries per page. Defaults to 50 if omitted. */
  limit: number;
}

export interface HistoryEntry {
  /** UUID of this history entry row. */
  id: string;
  /** Monotonically increasing per-application sequence number. */
  sequence: number;
  /** Human-readable change description. */
  description: string;
  /** ISO 8601 timestamp of when this entry was recorded. */
  createdAt: string;
  /** JSON-encoded snapshot; the caller computes field-level diffs. */
  snapshot: Uint8Array;
}

export interface ListHistoryResponse {
  entries: HistoryEntry[];
  total: number;
  page: number;
  limit: number;
}

export interface GetSnapshotAtVersionRequest {
  applicationId: string;
  sequence: number;
}

export interface GetSnapshotAtVersionResponse {
  /** JSON-encoded snapshot at the requested version. Empty if not found. */
  snapshot: Uint8Array;
  /** Whether the requested sequence was found. */
  found: boolean;
}

export interface DeleteHistoryRequest {
  applicationId: string;
}

export interface DeleteHistoryResponse {
  /** Number of history entries deleted. */
  deletedCount: number;
}

export const HISTORY_V1_PACKAGE_NAME = "history.v1";

function createBaseRecordHistoryRequest(): RecordHistoryRequest {
  return { applicationId: "", description: "", snapshot: new Uint8Array(0) };
}

export const RecordHistoryRequest: MessageFns<RecordHistoryRequest> = {
  encode(message: RecordHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.applicationId !== "") {
      writer.uint32(10).string(message.applicationId);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.snapshot.length !== 0) {
      writer.uint32(26).bytes(message.snapshot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecordHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecordHistoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.applicationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.snapshot = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseRecordHistoryResponse(): RecordHistoryResponse {
  return { sequence: 0 };
}

export const RecordHistoryResponse: MessageFns<RecordHistoryResponse> = {
  encode(message: RecordHistoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sequence !== 0) {
      writer.uint32(8).int32(message.sequence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecordHistoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecordHistoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sequence = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseListHistoryRequest(): ListHistoryRequest {
  return { applicationId: "", page: 0, limit: 0 };
}

export const ListHistoryRequest: MessageFns<ListHistoryRequest> = {
  encode(message: ListHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.applicationId !== "") {
      writer.uint32(10).string(message.applicationId);
    }
    if (message.page !== 0) {
      writer.uint32(16).int32(message.page);
    }
    if (message.limit !== 0) {
      writer.uint32(24).int32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListHistoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.applicationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.page = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseHistoryEntry(): HistoryEntry {
  return { id: "", sequence: 0, description: "", createdAt: "", snapshot: new Uint8Array(0) };
}

export const HistoryEntry: MessageFns<HistoryEntry> = {
  encode(message: HistoryEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.sequence !== 0) {
      writer.uint32(16).int32(message.sequence);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.createdAt !== "") {
      writer.uint32(34).string(message.createdAt);
    }
    if (message.snapshot.length !== 0) {
      writer.uint32(42).bytes(message.snapshot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HistoryEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHistoryEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sequence = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.snapshot = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseListHistoryResponse(): ListHistoryResponse {
  return { entries: [], total: 0, page: 0, limit: 0 };
}

export const ListHistoryResponse: MessageFns<ListHistoryResponse> = {
  encode(message: ListHistoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      HistoryEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.total !== 0) {
      writer.uint32(16).int32(message.total);
    }
    if (message.page !== 0) {
      writer.uint32(24).int32(message.page);
    }
    if (message.limit !== 0) {
      writer.uint32(32).int32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListHistoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListHistoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(HistoryEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.page = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseGetSnapshotAtVersionRequest(): GetSnapshotAtVersionRequest {
  return { applicationId: "", sequence: 0 };
}

export const GetSnapshotAtVersionRequest: MessageFns<GetSnapshotAtVersionRequest> = {
  encode(message: GetSnapshotAtVersionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.applicationId !== "") {
      writer.uint32(10).string(message.applicationId);
    }
    if (message.sequence !== 0) {
      writer.uint32(16).int32(message.sequence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSnapshotAtVersionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSnapshotAtVersionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.applicationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sequence = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseGetSnapshotAtVersionResponse(): GetSnapshotAtVersionResponse {
  return { snapshot: new Uint8Array(0), found: false };
}

export const GetSnapshotAtVersionResponse: MessageFns<GetSnapshotAtVersionResponse> = {
  encode(message: GetSnapshotAtVersionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.snapshot.length !== 0) {
      writer.uint32(10).bytes(message.snapshot);
    }
    if (message.found !== false) {
      writer.uint32(16).bool(message.found);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSnapshotAtVersionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSnapshotAtVersionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.snapshot = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseDeleteHistoryRequest(): DeleteHistoryRequest {
  return { applicationId: "" };
}

export const DeleteHistoryRequest: MessageFns<DeleteHistoryRequest> = {
  encode(message: DeleteHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.applicationId !== "") {
      writer.uint32(10).string(message.applicationId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteHistoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.applicationId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseDeleteHistoryResponse(): DeleteHistoryResponse {
  return { deletedCount: 0 };
}

export const DeleteHistoryResponse: MessageFns<DeleteHistoryResponse> = {
  encode(message: DeleteHistoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deletedCount !== 0) {
      writer.uint32(8).int32(message.deletedCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteHistoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteHistoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deletedCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

/**
 * HistoryService manages application history snapshots for the application tracker.
 * Snapshots are transported as opaque JSON bytes; only the caller interprets their contents.
 */

export interface HistoryServiceClient {
  /** RecordHistory writes one history entry for an application. */

  recordHistory(request: RecordHistoryRequest): Observable<RecordHistoryResponse>;

  /** ListHistory returns paginated history entries for an application, ordered by sequence descending. */

  listHistory(request: ListHistoryRequest): Observable<ListHistoryResponse>;

  /**
   * GetSnapshotAtVersion returns the snapshot at a specific sequence number.
   * Used by the caller to retrieve a target state for restore operations.
   */

  getSnapshotAtVersion(request: GetSnapshotAtVersionRequest): Observable<GetSnapshotAtVersionResponse>;

  /**
   * DeleteHistory removes all history entries for an application.
   * Called when an application is deleted (cross-schema cascade replacement).
   */

  deleteHistory(request: DeleteHistoryRequest): Observable<DeleteHistoryResponse>;
}

/**
 * HistoryService manages application history snapshots for the application tracker.
 * Snapshots are transported as opaque JSON bytes; only the caller interprets their contents.
 */

export interface HistoryServiceController {
  /** RecordHistory writes one history entry for an application. */

  recordHistory(
    request: RecordHistoryRequest,
  ): Promise<RecordHistoryResponse> | Observable<RecordHistoryResponse> | RecordHistoryResponse;

  /** ListHistory returns paginated history entries for an application, ordered by sequence descending. */

  listHistory(
    request: ListHistoryRequest,
  ): Promise<ListHistoryResponse> | Observable<ListHistoryResponse> | ListHistoryResponse;

  /**
   * GetSnapshotAtVersion returns the snapshot at a specific sequence number.
   * Used by the caller to retrieve a target state for restore operations.
   */

  getSnapshotAtVersion(
    request: GetSnapshotAtVersionRequest,
  ): Promise<GetSnapshotAtVersionResponse> | Observable<GetSnapshotAtVersionResponse> | GetSnapshotAtVersionResponse;

  /**
   * DeleteHistory removes all history entries for an application.
   * Called when an application is deleted (cross-schema cascade replacement).
   */

  deleteHistory(
    request: DeleteHistoryRequest,
  ): Promise<DeleteHistoryResponse> | Observable<DeleteHistoryResponse> | DeleteHistoryResponse;
}

export function HistoryServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["recordHistory", "listHistory", "getSnapshotAtVersion", "deleteHistory"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("HistoryService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("HistoryService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const HISTORY_SERVICE_NAME = "HistoryService";

/**
 * HistoryService manages application history snapshots for the application tracker.
 * Snapshots are transported as opaque JSON bytes; only the caller interprets their contents.
 */
export type HistoryServiceService = typeof HistoryServiceService;
export const HistoryServiceService = {
  /** RecordHistory writes one history entry for an application. */
  recordHistory: {
    path: "/history.v1.HistoryService/RecordHistory",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: RecordHistoryRequest) => Buffer.from(RecordHistoryRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => RecordHistoryRequest.decode(value),
    responseSerialize: (value: RecordHistoryResponse) => Buffer.from(RecordHistoryResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => RecordHistoryResponse.decode(value),
  },
  /** ListHistory returns paginated history entries for an application, ordered by sequence descending. */
  listHistory: {
    path: "/history.v1.HistoryService/ListHistory",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: ListHistoryRequest) => Buffer.from(ListHistoryRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => ListHistoryRequest.decode(value),
    responseSerialize: (value: ListHistoryResponse) => Buffer.from(ListHistoryResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => ListHistoryResponse.decode(value),
  },
  /**
   * GetSnapshotAtVersion returns the snapshot at a specific sequence number.
   * Used by the caller to retrieve a target state for restore operations.
   */
  getSnapshotAtVersion: {
    path: "/history.v1.HistoryService/GetSnapshotAtVersion",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: GetSnapshotAtVersionRequest) =>
      Buffer.from(GetSnapshotAtVersionRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => GetSnapshotAtVersionRequest.decode(value),
    responseSerialize: (value: GetSnapshotAtVersionResponse) =>
      Buffer.from(GetSnapshotAtVersionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => GetSnapshotAtVersionResponse.decode(value),
  },
  /**
   * DeleteHistory removes all history entries for an application.
   * Called when an application is deleted (cross-schema cascade replacement).
   */
  deleteHistory: {
    path: "/history.v1.HistoryService/DeleteHistory",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: DeleteHistoryRequest) => Buffer.from(DeleteHistoryRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => DeleteHistoryRequest.decode(value),
    responseSerialize: (value: DeleteHistoryResponse) => Buffer.from(DeleteHistoryResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => DeleteHistoryResponse.decode(value),
  },
} as const;

export interface HistoryServiceServer extends UntypedServiceImplementation {
  /** RecordHistory writes one history entry for an application. */
  recordHistory: handleUnaryCall<RecordHistoryRequest, RecordHistoryResponse>;
  /** ListHistory returns paginated history entries for an application, ordered by sequence descending. */
  listHistory: handleUnaryCall<ListHistoryRequest, ListHistoryResponse>;
  /**
   * GetSnapshotAtVersion returns the snapshot at a specific sequence number.
   * Used by the caller to retrieve a target state for restore operations.
   */
  getSnapshotAtVersion: handleUnaryCall<GetSnapshotAtVersionRequest, GetSnapshotAtVersionResponse>;
  /**
   * DeleteHistory removes all history entries for an application.
   * Called when an application is deleted (cross-schema cascade replacement).
   */
  deleteHistory: handleUnaryCall<DeleteHistoryRequest, DeleteHistoryResponse>;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
