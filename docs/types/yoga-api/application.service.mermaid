classDiagram
  %% Legend
  %% --|> : Inheritance (extends)
  %% ..|> : Implementation (implements)
  %% --* : Composition (has/contains)
  %% -- : Association

  %% Errors encountered during conversion:
  %% - TS2307: Cannot find module '../db/client.js' or its corresponding type declarations.
  %% - TS2307: Cannot find module '@prisma/client' or its corresponding type declarations.
  %% - TS2307: Cannot find module './history.service.js' or its corresponding type declarations.

  class ListFilters {
    <<interface>>
    -status?: ApplicationStatus
    -companyCategory?: CompanyCategory
    -jobSource?: JobSource
    -skillsMatchMin?: number
    -includeArchived?: boolean
    -sortBy?: string
    -sortDir?: string
  }

  class Pagination {
    <<interface>>
    -page?: number
    -limit?: number
  }

