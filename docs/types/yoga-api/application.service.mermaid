%% Yoga API type diagram intentionally omitted.
%% The previous generated diagram had unresolved imports during ts-to-mermaid
%% conversion, which made it unreliable as documentation.
%%
%% Once the generation pipeline has been fixed so that all imports resolve
%% cleanly, regenerate this file from the TypeScript sources and restore
%% a proper `classDiagram` definition here.
